# Node task server config — copy to .env if you want to override defaults.

PORT=9914

# Where task metadata + logs are stored (JSON files). Defaults to apps/server/data.
# DATA_DIR=D:\01.src\home\ai-task-router\apps\server\data

# CLI binaries — override if not on PATH.
# CLAUDE_BIN=claude
# CODEX_BIN=codex

# Passed as `claude -p ... --permission-mode <this>`.
# acceptEdits: auto-accepts file edits, still gates other tool use.
# CLAUDE_PERMISSION_MODE=acceptEdits

# Optional Codex model override.
# CODEX_MODEL=

# CORS origin allowed to call this server (the Next.js dev server).
WEB_ORIGIN=http://localhost:9913

# Show the Codex account's email on the usage panel.
# Off by default: that email is the only reason to open ~/.codex/auth.json,
# and that file also holds live access/refresh tokens. Left off, no credential
# file is read at all and only the account label is missing — the Codex plan
# still shows, and Claude's account is unaffected (its profile file has no
# tokens). See docs/usage-panel.md.
# USAGE_SHOW_ACCOUNT=true
